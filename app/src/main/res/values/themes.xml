<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
        Splash/launch theme. Compose draws its own Material 3 theme in code
        (see ui/theme/Theme.kt) — this XML theme only controls the initial
        window style before the activity's content is composed.
    -->
    <style name="Theme.GymTracker" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>